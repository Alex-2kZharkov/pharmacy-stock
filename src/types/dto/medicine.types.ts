export interface MedicineDto {
  _id: string;
  createdAt: string;
  updatedAt: string;
  name: string;
  quantity: number;
  primaryAmount: number;
  finalAmount: number;
  percent: number;
  prognosis: number;
}
