import { makeStyles } from "@material-ui/core";

// import { MAIN } from "../../../theme/colors/colors.constants";

export const useStyles = makeStyles((theme) => ({
  card: {
    width: "100%",
    padding: theme.spacing(2.5, 3),
  },
}));
